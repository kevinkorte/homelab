# Paste into the Komodo Stack "Environment" field.
#
# This stack takes no variables — uptime-kuma is configured entirely through its
# own UI, and its timezone is a per-install setting rather than a TZ env var.
# Intentionally empty; leave Komodo's Environment field blank.
