# Paste into the Komodo Stack "Environment" field.
#
# This stack takes no variables. Both services are configured entirely by their
# `command:` and the shared data directory.
# Intentionally empty; leave Komodo's Environment field blank.
